# scripts — Agent 指令与脱敏工具

本目录存放 JKBuddy 发布时用到的辅助脚本（脱敏公开版）。

| 脚本 | 用途 |
|------|------|
| `sanitize_jkbuddy.js` | 批量替换仓库中的真实企业名 / IP / 用户名，生成公开脱敏版本 |
| `list_agents.js` | 列出 LibreChat agents 集合中的智能体（示例，连接串已脱敏） |

> ⚠️ 安全提醒：所有脚本中的数据库连接串、密钥均以 `<占位符>` 形式给出。
> 部署时请勿将真实凭据提交到版本库。
