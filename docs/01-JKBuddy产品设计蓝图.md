# JKBuddy 产品设计蓝图

> **文档版本**：v1.0
> **生成日期**：2026-08-08
> **文档类型**：产品设计蓝图（总览）
> **状态**：待评审（仅审阅，未改动生产）
> **核心模型**：**qwen3.6-35B（本地部署）**（取代此前讨论中的 gemma-4-26B）
> **定位**：企业级「AI 问数 + 业务办事」平台蓝图，整合 LibreChat（多 Agent 对话中枢）、MCP（能力接入）、代码沙箱、若依权限、统一门户 SSO。

> **配套文档**（编号体系）：
> - [01 本蓝图](01-JKBuddy产品设计蓝图.md)（本文件，总览）
> - [02-投资问数多Agent架构设计.md](02-投资问数多Agent架构设计.md)（A1/A2 分层共享 + 防重）
> - [03-LibreChat落地操作指南.md](03-LibreChat落地操作指南.md)（部署）
> - [04-本体层落地与组件边界设计.md](04-本体层落地与组件边界设计.md)（本体边界）
> - [05-A1设计方案.md](05-A1设计方案.md)（A1 分层指令 + A3-A6）
> - [06-若依系统MCP化改造与数据权限设计.md](06-若依系统MCP化改造与数据权限设计.md)（MCP 化 + 数据权限，资产系统样例）
> - [07-本体架构策略（共享核心+领域扩展）.md](07-本体架构策略（共享核心+领域扩展）.md)（本体组织：共享核心 + 领域扩展）
> - [08-承载平台迁移与扩展路线图.md](08-承载平台迁移与扩展路线图.md)（承载平台迁移/扩展路线，P0-P4）

---

## 1. 总体定位与核心判断

JKBuddy 的本质是**「企业级 AI 问数与业务办事平台」**，不是单纯聊天工具。其核心价值不在模型能力，而在 **LibreChat 多 Agent 编排 + 若依数据权限 + 统一门户身份 + 代码沙箱** 这套"受控的智能体接入层"。

**设计主轴（四不可割裂）**：
1. **身份与权限贯穿所有 Agent**
2. **能力以 MCP 工具为唯一入口**
3. **一切写操作经若依二次裁决**
4. **数据权限判定完全收敛在若依服务端**（agent/MCP 不做分类识别）

一旦割裂，产品会退化成"模型直连数据库"的裸查询，丧失企业适配性。

---

## 2. 技术底座

| 组件 | 选型 | 职责 |
|------|------|------|
| 对话中枢 | LibreChat | 多 Agent 编排、会话、记忆、MCP 挂载 |
| **核心模型** | **qwen3.6-35B（本地部署）** | 意图分类 / 工具调用 / 输出生成 |
| 能力接入 | MCP Server（每业务系统独立） | 查询 / 审批 / 多系统协同工具 |
| 权限系统 | 若依（RuoYi） | 功能权限 @PreAuthorize + 数据权限 @DataScope |
| 身份认证 | 统一门户 SSO（蓝凌 OAuth2） | 单点登录、身份透传 |
| 执行沙箱 | LibreChat 代码沙箱（code-command） | 文件导出、可视化、受限计算 |
| 数据底座 | 若依业务库 + DWD 只读视图 | 数据存储与预聚合 |

### 2.0 技术底座选型原则：为什么复用现有 Agent 工具而非从零自建

JKBuddy 采用现成 Agent 工具（LibreChat）作为底座，而非从零自建 buddy 工具，**不是妥协，而是与产品本质对齐的理性选择**。核心判断：**轮子复用，差异化内核自研**。

**JKBuddy 的价值不在"聊天引擎"，而在"受控的智能体接入层"**——若依数据权限、统一门户身份、本体语义、业务 MCP。聊天引擎是成熟、庞大、持续演进的海量轮子，从零自建是巨大的时间与维护成本沉没。

| 视角 | 自建的理由（反方） | 复用的理由（正方） | 判词 |
|------|------|------|------|
| 时间成本 | 无外部依赖、完全可控 | 多 Agent 编排/会话/记忆/MCP/安全/前端 UI 均为海量轮子，自建需数月~数年且持续维护 | 复用压倒性胜出 |
| 成熟度 | 无历史包袱 | Chain/Handoff、MCP、记忆、沙箱已被社区大量验证 | 复用胜出 |
| 定制自由度 | 完全掌控，不受 §11 受限点约束 | 受限点可通过升级/迁移到更完整平台解决（见 §11 与 08） | 受限点不构成自建理由 |
| 安全可控 | 代码全自研、审计彻底 | 开源可审计 + 企业定制层自研 | 平手，靠定制层保障 |
| 核心价值聚焦 | 想掌控一切 | JKBuddy 差异化在企业适配内核，不在聊天引擎 | 复用让资源聚焦差异化 |

**结论（辩证收敛）**：LibreChat 提供"通用引擎底座"，JKBuddy 自研"企业适配内核"。这同时解释了为什么 §11 受限点（渲染/Artifact、审批工作流、本体治理）要"依托更完整平台"而非"推倒重来自建"——那些仍是平台级能力，复用一个更强的平台仍是复用逻辑。

**可由现成 Agent 产品直接获取的既有能力**（JKBuddy 只需定制/扩展，无需重造）：

| # | 既有能力 | 由现成产品提供 | JKBuddy 如何复用与边界 | 是否需定制 |
|---|---------|--------------|----------------------|-----------|
| 1 | 对话/会话管理 | LibreChat | 多会话、上下文、历史、分享，作为对话中枢底座 | 否 |
| 2 | 多 Agent 编排 | LibreChat | A1-A6 通过 Chain / Handoff / Subagents 串联（见 §4 / 05） | 是（编排配置） |
| 3 | MCP 能力接入 | LibreChat | 工具挂载 + `allowed-tools` 收敛到各 Agent | 是（按 Agent 收敛） |
| 4 | 身份认证 | LibreChat | OAuth2/OpenID/JWT/LDAP/SAML/本地多策略，叠加统一门户 SSO | 是（接蓝凌） |
| 5 | RAG/文件/记忆 | LibreChat | 向量检索、音频/OCR/文本解析、长期记忆 | 否 |
| 6 | 模型路由 | LibreChat | 多端点接入（本地 qwen3.6-35B + 云端）、capabilities | 是（本地端点） |
| 7 | 前端 UI | LibreChat | 聊天界面、Agent 配置面板、工具面板 | 是（政务端庄风定制） |
| 8 | 代码沙箱 | LibreChat | code-command 自托管执行（导出/可视化） | 否 |
| 9 | 可视化 | LibreChat | Mermaid 图表等（见 Skill） | 否 |
| 10 | 搜索索引 | LibreChat（MeiliSearch） | 对话/文件检索 | 否 |
| 11 | 安全治理 | LibreChat | 限流、审计、API Keys、邀请、角色、禁令 | 否 |

> **复用边界**：上表"是否需定制"为否的能力，JKBuddy 直接使用其原生能力，不做框架层修改；"是"的能力是 JKBuddy 差异化投入点，但也只是**配置/挂载/样式层定制**，非底层重造。

### 2.1 模型选型说明（qwen3.6-35B 本地部署）

- 取代此前讨论的 gemma-4-26B，作为 A1/A2 及下游 Agent 的默认推理模型。
- 因模型改为 **35B 本地部署**，需重新验证以下点（原防重机制仍须保留）：
  - 工具调用（function calling）的稳定性；
  - 多轮澄清是否仍会重复提问 → 保留 `recursion_limit` 硬限制与 `end_after_tools=true`（02/05 已固化）；
  - 本地推理延迟对并发的影响（见 §9）。
- 模型能力与具体微调细节不在本蓝图臆测范围，落地时以实测为准。

### 2.2 本体设计原则

本体采用**混合方案（共享核心 + 领域扩展）**，详见 [07-本体架构策略](07-本体架构策略（共享核心+领域扩展）.md)。核心原则性表述如下：

1. **共享核心管跨系统稳定语义**：企业/部门/人员/组织、审批(approval)、数据权限(data_scope)、通用枚举(金额单位/业务线骨架) 等**与数据源无关**的概念，抽离为 `platform` 共享核心本体，供所有业务系统复用。
2. **领域本体管绑定数据源的具体语义**：投资(Fund/Subfund/Project + DWD 白名单)、资产(资产/处置) 等绑定具体表/视图的语义，放在各业务系统自己的领域本体，**延续现有 `ontology.yaml`**。
3. **判定准则**：**是否绑定数据源（view/table）**——绑定 → 领域，不绑定 → 共享核心；含视图级安全白名单的必属领域。
4. **依赖单向**：领域本体 `imports` 共享核心，**禁止领域间互引**，避免依赖环。
5. **Skill 不持有语义**：Skill 仅做"本体→MCP 操作适配"，语义单一事实源始终在本体（延续 04 Q3）。
6. **版本治理**：共享核心严格版本化 + 中央评审；领域本体锁定核心版本、独立发布。

---

## 3. 核心组件集成与数据流转

**产品架构图**（含本体横切层）：

```
┌────────────────────────────────────────────────────────────────────┐
│                        统一门户（SSO 单点登录）                       │
│                      OAuth2 认证 · 身份透传(loginName)              │
└───────────────────────────────┬────────────────────────────────────┘
                                ▼
┌────────────────────────────────────────────────────────────────────┐
│                      LibreChat（对话中枢 / 多 Agent）                │
│    A1 意图路由 → A2 数据 / A3 系统 / A4 导出 / A5 审批 / A6 协同      │
│    会话/记忆(MongoDB) · qwen3.6-35B                                 │
└──────────┬───────────────────────────┬─────────────────────────────┘
           │ MCP 协议(携带身份,仅透传)      │ 生成 Web 页面 / 渲染指令
           ▼                             ▼
┌──────────────────────┐      ┌────────────────────────────────────┐
│  MCP Server(每系统独立) │      │  H5 渲染 / Artifact（AI 生成页面）   │
│  mcp-inv-server-v2(投资)│      │  · 系统 Web 页面 AI 生成            │
│  mcp-asset-server(资产)│      │  · 统一应用入口 · 交互式渲染         │
│  └─ 查询/审批/协同 工具   │      │  · 代码沙箱(code-command)承载      │
└───────────┬──────────┘      └────────────────────────────────────┘
            ▼  REST（身份 + 业务参数）
┌────────────────────────────────────────────────────────────────────┐
│                        若依（RuoYi）                                │
│   @PreAuthorize 功能鉴权 · @DataScope 数据权限判定（收敛在此）         │
│   业务 Service / 审批流程 / 多系统协同                               │
└───────────────┬────────────────────────────────────────────────────┘
                ▼
┌────────────────────────────────────────────────────────────────────┐
│                      数据底座（各业务库 + DWD 只读视图）              │
│   投资 <db-name> / 资产 <asset-db>                                  │
└────────────────────────────────────────────────────────────────────┘

横切层（贯穿全链路）：
  · 本体层：platform 共享核心 ← domain:investment / domain:asset（import 单向依赖，见 07）
  · 安全层：身份校验 · 参数白名单 · 数据权限 · 操作审计
  · 渲染层：AI 生成 H5/Artifact 页面，作为统一应用入口（LibreChat 渲染能力受限，需依托更完整平台，见 §11）
```

**架构形态**：**星型总线**——LibreChat 为对话中枢，所有能力以 MCP 工具为边界，避免 Agent 直连业务库。

**数据流转链路**：
```
统一门户(SSO) → LibreChat AuthContext → 会话上下文(identity)
   → A1 意图路由 → 下游 Agent
   → MCP Server（携带已认证身份，仅透传）
   → 若依：@PreAuthorize + @DataScope（数据权限判定）
   → 返回受控数据 / 执行审批 / 多系统协同
   → 代码沙箱（仅对已授权结果做导出/可视化）
```

**关键约束**：
- LibreChat ↔ MCP：经 mcpServers 挂载，工具以 `allowed-tools` 收敛到各 Agent。**MCP 工具是"业务能力点"，不是"模型自由调用点"**。
- 身份透传：MCP 仅透传已认证 loginName，**数据权限判定在若依**（06 §4）。
- 代码沙箱：仅接收 A2/A4 已授权结果，**不接触原始库**。

**潜在风险**：
- **身份伪造**：MCP 信任客户端自报 loginName → 越权。对策：仅信任 LibreChat 服务端注入的认证上下文，MCP 中间件校验会话有效（06 §4.6）。
- **工具暴露面过大**：MCP 工具注册过多 → 模型误用。对策：按 Agent 收敛 `allowed-tools` + 工具 docstring 声明边界（04 本体边界）。

### 3.1 受控的智能体接入层：网关统一调度 + 本体注入（扩展设计）

**定位**：JKBuddy 作为"受控的智能体接入层"，本质是一个**受控网关**——所有请求经统一入口进入，经鉴权、本体语义注入、意图路由后，分派给对应 Agent，再由 Agent 经受控 MCP 工具访问若依。本小节以简化的伪代码与调用链展示这一"网关统一调度 + 本体注入"机制。

**核心不变量（接入层铁律）**：
1. **单入口**：一切 Agent 调用都从网关入口进入，无旁路。
2. **身份服务端解析**：loginName → sys_user 由网关/MCP 服务端解析，客户端不报身份。
3. **本体注入**：每个 Agent 会话注入其所属领域的本体上下文（platform 共享 + domain 领域），语义有单一事实源（见 07）。
4. **数据权限判定收敛在若依**：网关/Agent/MCP 不做分类识别，仅透传已认证身份（06 核心原则）。
5. **工具受控**：Agent 只能经 `allowed-tools` 收敛后的 MCP 工具访问能力，不直连业务库。

**架构草图（受控网关形态）**：
```
                  ┌─────────────────────────────────────────────┐
  用户请求 ──────▶│   JKBuddy 受控接入层网关（Gateway）            │
                  │  · 鉴权（loginName → sys_user）              │
                  │  · 会话上下文（identity + domain）            │
                  │  · 本体注入（platform + domain 语义段）       │
                  │  · 意图路由（A1 分类器）                      │
                  └──────────────┬──────────────────────────────┘
                                 │ 路由分发（按 L0/L1 分类）
        ┌────────────┬───────────┼───────────┬─────────────┐
        ▼            ▼           ▼           ▼             ▼
      A2 数据      A3 系统     A4 导出     A5 审批      A6 协同
      执行器        处理        沙箱        处理          协同
        │            │           │           │             │
        └────────────┴───────────┼───────────┴─────────────┘
                                 ▼  受控 MCP 工具（allowed-tools 收敛）
                          ┌──────────────────────────────┐
                          │  MCP Server（每业务系统独立）    │
                          │  · 身份透传（仅 loginName）     │
                          └──────────────┬───────────────┘
                                         ▼ REST（身份+业务参数）
                          ┌──────────────────────────────┐
                          │  若依：@PreAuthorize + @DataScope │
                          │  （数据权限判定收敛在此）         │
                          └──────────────────────────────┘
```

**伪代码（网关统一调度 + 本体注入，示意）**：

```python
# JKBuddy 受控接入层网关 —— 简化示意伪代码
# 不变量：单入口 / 身份服务端解析 / 本体注入 / 权限判定在若依 / 工具受控

class BuddyGateway:
    """受控的智能体接入层网关：统一调度多个 Agent 并注入本体语义。"""

    def __init__(self, ontology_registry, agent_registry, mcp_gateway):
        self.ontology_registry = ontology_registry   # 本体注册表（platform + domain）
        self.agent_registry   = agent_registry       # Agent 注册表（A1-A6）
        self.mcp_gateway      = mcp_gateway          # 受控 MCP 工具网关（allowed-tools 收敛）

    def handle(self, request):
        # ── 1. 鉴权（服务端解析身份，客户端不自报）──
        identity = self._resolve_identity(request.loginName)   # loginName → sys_user，校验存在/启停
        if identity is None:
            return 401

        # ── 2. 本体语义注入：为本次会话装配 platform + 所属领域本体 ──
        domain = self._detect_domain(request)                  # 由请求/历史判定业务域（投资/资产…）
        ontology = self.ontology_registry.merge("platform", domain)
        # 注入到会话上下文：Agent 的指令/Skill 引用本体生成的人读投影，机读白名单进 MCP

        # ── 3. 意图路由：A1 纯分类器（零工具）──
        route = self.agent_registry["A1"].classify(request.text, ontology=ontology)
        # route = {L0, L1, params, target_agent}；A1 只输出结构化分类，不做查询

        # ── 4. 分派下游 Agent（交接契约：恰一次、之后停止，见 05）──
        agent = self.agent_registry[route.target_agent]
        result = agent.run(
            request=request,
            identity=identity,          # 仅透传已认证身份（不报 data_scope）
            ontology=ontology,          # 注入本体语义
            tools=self._allowed_tools(agent),   # 该 Agent 收敛后的 MCP 工具集
        )

        # ── 5. 受控执行：Agent 仅经 allowed-tools 调 MCP，MCP 透传身份到若依判定 ──
        # 数据权限判定收敛在若依（@PreAuthorize + @DataScope），网关/Agent 不做分类识别
        return self._to_response(result, identity, route)

    def _allowed_tools(self, agent):
        """按 Agent 收敛 MCP 工具：MCP 工具是'业务能力点'，不是'模型自由调用点'。"""
        return [t for t in self.mcp_gateway.tools() if t in agent.allowed_tools]
```

**调用链（一次查询）**：
```
用户 → Gateway.handle(loginName, text)
  → resolve_identity(loginName) ── 服务端解析身份
  → merge_ontology(platform + domain) ── 注入本体语义
  → A1.classify(text, ontology) ── 意图路由（L0/L1/参数/target_agent）
  → A2.run(text, identity, ontology, allowed_tools) ── 数据执行
      → mcp_gateway.invoke(tool, identity, params) ── 受控工具调用，仅透传身份
          → RuoYi: @PreAuthorize + @DataScope ── 数据权限判定（收敛在此）
      → 返回受控数据
  → 网关封装响应（含数据来源/条数/时间戳）
```

**该设计如何体现"受控"**：
- **身份受控**：身份解析与服务端，客户端不可伪造（防越权）。
- **语义受控**：本体注入让 Agent 的认知有单一事实源，非模型自由臆测。
- **权限受控**：数据权限判定完全收敛在若依，接入层只透传（口径不漂移）。
- **工具受控**：`allowed-tools` 收敛 + MCP 为唯一能力边界，Agent 不直连业务库。

---

## 4. 多 Agent 系统实现架构

**架构原则**：分层共享 + 职责收敛（05 文档）。

| Agent | 类型 | 工具 | 职责 |
|-------|------|------|------|
| A1 意图路由器 | 分类器（零工具） | 无 | L0 意图门 + 参数提取 + 交接 |
| A2 数据执行器 | 执行器 | 查询类 MCP | 调工具查数、数据忠实输出 |
| A3 系统处理 | 执行器 | 系统/澄清 | 元请求、歧义澄清、无关说明 |
| A4 文件导出 | 执行器 | 代码沙箱 | 基于已授权结果导出 |
| A5 审批处理 | 执行器 | 审批类 MCP | 审批流转（若依裁决） |
| A6 跨系统协同 | 执行器 | 协同类 MCP | 跨系统动作 |

**任务分配与协作**：A1 通过 Chain/Handoff 分派；**交接契约"恰一次、之后停止"**（05 §4），杜绝 A1 死循环。

**冲突解决**：
- 工具竞争 → `end_after_tools` + 每 Agent 收敛工具集。
- 语义冲突（政府引导基金拆解）→ 固化于 A1 指令/Skill，单一事实源（04 本体）。
- 角色冲突（领导/员工/审计）→ Chain 上下文传角色，A2 角色感知输出（02）。

**潜在风险**：
- Agent 级联放大幻觉 → A1 输出结构化（L0/L1/参数/目标Agent）+ 可观测标记。
- 递归失控 → `recursion_limit` 硬限制 + `end_after_tools=true`。

---

## 5. 统一门户单点登录（SSO）

**技术选型**：蓝凌 SSO + OAuth2 授权码模式。LibreChat 为 OAuth2 Client，统一门户为 IdP。

**认证流程**：
```
未登录访问 → 重定向统一门户 → 门户认证 → 回调带 code
 → LibreChat 换 token → 拉用户信息 → 建立本地会话(loginName)
 → 后续请求携带身份 → MCP 透传 → 若依判定
```

**集成要点**：
- 双开关：`MK_SSO_ENABLED`（后端）+ `VITE_MK_SSO_ENABLED`（前端跳转）；**VITE_ 为构建期变量，改动须重建前端**。
- SSO 管理员名单：`MK_SSO_ADMIN_ACCOUNTS`。
- 会话过期：`SESSION_EXPIRY` 按企业安全要求调整。

**潜在风险**：
- 认证绕过 → `MK_SSO_ENABLED=false` 时同步不注册 `/ssoLogin` 路由。
- 缓存陈旧用户 → 改用户文档须使会话缓存失效，避免 OpenID JWT 突发缓存返回旧 `req.user`。

---

## 6. 权限管理细粒度控制

**双层权限**（全部由若依裁决，06 核心原则）：
- **功能权限**：菜单/操作 → `@PreAuthorize`。
- **数据权限**：`data_scope`（1全部/2自定义/3本部门/4本部门及以下/5仅本人）→ `@DataScope`。
- **能力权限**：MCP 工具按 Agent 收敛（`allowed-tools`）。

**用户角色**：领导 / 员工 / 审计 三类画像（02），映射到若依 `sys_role`。

**操作审计**：每次查询/审批/协同留痕（发起人、范围、时间）。

**潜在风险**：
- 口径漂移 → 强约束"判定收敛在若依，agent/MCP 不做分类识别"。
- 越权代批 → 审批一律经若依 REST，`@PreAuthorize`+`@DataScope` 双校验。

---

## 7. 功能优先级与迭代路线

**MVP（最小可行产品）**：
1. 单点登录 + 用户身份打通
2. A1 意图路由 + A2 基础查询（若依权限受控）
3. 表格/文字输出（数据忠实）
4. 审批状态只读查询（待办/状态）

**P1（增强）**：审批动作、文件导出、多系统协同。
**P2（规模化）**：跨系统协同深化、审计报表、性能优化。

**理由（辩证）**：先证明「权限受控的查询」闭环可行，再上写操作，避免早期暴露审批/协同高风险面。

**潜在风险**：MVP 范围蔓延 → 用"每个版本一个可验证闭环"约束。

---

## 8. 用户体验设计

- **交互流程**：未登录一键跳转门户，回跳免重复登录；对话即入口，Agent 路由透明；歧义最小澄清（≤1 轮、给候选）。
- **界面布局**（政务端庄风，蓝 #1e3a5f + 金 #d4af37）：左导航路径树（逐级累积高亮）+ 主对话区 + 结果表格区；结果附数据来源/条数/时间戳。
- **响应反馈**：工具调用中显示"查询中"；无数据显示"无数据"而非编造；审批成功/403 明确反馈。
- **潜在风险**：过度拟人/冗长 → 结构化表格 + 关键洞察（领导版 3 行洞察）。

---

## 9. 性能与可扩展性

- **并发处理**：LibreChat/MCP 无状态 → 横向扩容；后端数据库并行读取（避免串行往返）。
- **数据存储**：业务数据在若依库；DWD/本体只读视图；会话/记忆在 LibreChat（MongoDB）；缓存短 TTL。
- **水平扩展**：每业务系统独立 MCP Server（06）→ 隔离故障域、独立扩缩；MCP 多实例 + 负载均衡；认证无状态（JWT）。
- **模型推理（qwen3.6-35B 本地）**：关注本地推理吞吐对并发的制约，必要时做并发限流/排队，避免推理队列打爆。
- **潜在风险**：大数据量查询慢 → DWD 预聚合 + 索引（04）；沙箱资源耗尽 → 限时/限资源。

---

## 10. 安全防护体系

- **数据加密**：传输 TLS；敏感字段按权限脱敏；密钥存 `.env`/KMS，不入库不入 Git。
- **漏洞防护**：
  - 认证：OAuth2 + 服务端身份校验，防伪造；
  - 注入：MCP 工具参数白名单 + 参数化，防 SQL 注入；
  - 沙箱：隔离执行，防命令注入/路径穿越；
  - SSO：禁用时路由不注册，防旁路。
- **合规**：数据权限最小化；操作审计留痕；公开仓库脱敏（JKBuddy 规则）。
- **潜在风险**：安全与易用冲突 → 以"默认最小权限"为原则，特殊放行需显式配置。

---

## 11. 更完整平台诉求

### 11.1 LibreChat 受限点（目前发现，促发更完整平台）

在 JKBuddy 的设计与落地实践中，发现 LibreChat 在以下方面存在限制，成为**必须依托更完整承载平台**的动因：

| # | LibreChat 受限点 | 影响 | 诉求 |
|---|----------------|------|------|
| 1 | **渲染 / Artifact 能力弱**：默认以聊天 UI 为主，对"AI 生成可交互系统 Web 页面（H5/Artifact）"支持有限 | 难以作为统一应用入口提供丰富交互 | 需 H5 渲染 / Artifact 工具，AI 生成系统页面并承载交互（见 §3 渲染层） |
| 2 | **多 Agent 编排靠 Chain/Handoff 手工配置**：A1-A6 复杂业务流（审批/协同/跨系统）依赖指令与配置拼接 | 审批状态机、多系统事务难以在平台内表达 | 需平台级工作流 / 审批 / 任务编排引擎 |
| 3 | **MCP 工具是"能力点"非"流程"**：无法表达审批状态机、跨系统事务语义 | 写操作与协同缺乏流程语义 | 需平台级业务流程 / 状态管理 |
| 4 | **无内置权限模型**：数据权限依赖若依二次裁决（06），平台内缺少细粒度资源授权 | 权限治理分散在外部系统 | 需统一身份 / 授权 / 审计底座 |
| 5 | **无内置本体层**：语义靠 Skill/指令维护（04/07 引入外部本体方案） | 多系统本体统一需外部支撑 | 需平台级语义 / 本体治理 |
| 6 | **会话/记忆仅限聊天场景**：难以承载"统一应用入口"式的持久化应用状态 | 应用页状态、工作台难持续 | 需平台级应用状态 / 工作台承载 |
| 7 | **模型单一绑定**：本地模型 qwen3.6-35B 的 function calling、并发推理吞吐受限（§2.1/§9） | 大并发与复杂工具调用受限 | 需平台级模型路由 / 限流 / 弹性 |

> 上述受限点指向一个更完整的**承载平台**（工作流、权限、渲染/Artifact、本体治理、模型路由、统一应用入口），而**不改变** JKBuddy 已固化的业务设计（多 Agent、MCP 化、若依数据权限、混合本体）。

### 11.2 承载载体：openworker（后续探索）

> 后续将探索使用 **openworker** 作为 JKBuddy 的承载载体，以补齐 §11.1 受限点。
> ⚠️ 目前对 openworker 的产品定位、API、与 LibreChat/若依/沙箱的集成方式**尚无确切信息**，暂不臆测技术细节。
> **具体迁移/扩展路线见 [08-承载平台迁移与扩展路线图](08-承载平台迁移与扩展路线图.md)（P0-P4 分阶段，openworker/自建双轨、载体无关）**。

---

## 12. 与既有文档衔接

- 多 Agent 编排、防重、角色感知 → [02](02-投资问数多Agent架构设计.md)
- 部署落地 → [03](03-LibreChat落地操作指南.md)
- 本体边界、单一事实源 → [04](04-本体层落地与组件边界设计.md)
- A1 分层指令、A3-A6、交接契约 → [05](05-A1设计方案.md)
- 若依 MCP 化、数据权限（资产系统样例）→ [06](06-若依系统MCP化改造与数据权限设计.md)
- 本体组织（共享核心 + 领域扩展）→ [07](07-本体架构策略（共享核心+领域扩展）.md)
- 承载平台迁移/扩展路线 → [08](08-承载平台迁移与扩展路线图.md)

> 本文档为**总览蓝图**，各维度细则以对应编号文档为准；本蓝图统一各文档的**核心模型为 qwen3.6-35B 本地部署**。
